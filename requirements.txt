# Core requirements extracted from imports
# formatter.py must be present in your local directory

# Development and testing
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

# Optional: if you decide to use a more robust regex engine
regex>=2023.0.0